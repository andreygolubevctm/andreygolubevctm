package com.ctm.energy.quote.request.model.preferences;

import com.ctm.interfaces.common.types.ValueType;

public class DisplayDiscount extends ValueType<Boolean> {
    public DisplayDiscount(Boolean value) {
        super(value);
    }
}
